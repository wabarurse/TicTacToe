#ifndef BUTTON_HPP
#define BUTTON_HPP

#include "/opt/homebrew/include/SDL2/SDL.h"

// class Button {
//     private:
//         int x, y, length, width;
//     public:
//         Button(int x, int y, int length, int width);
//         void createButton(SDL_Renderer* renderer);
//         bool isClicked(int mouseX, int mouseY);
// };

#endif